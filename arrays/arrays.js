// Creating an array

let values = [];

//create and initilize an array
let value = [1, 2, 3];

console.log(value);

//accessing array items

const valuesOfArray = ['a', 'b', 'c'];

console.log(valuesOfArray[2]);

//changing values in a array

const changingArrayValues = ['sriman', 'rajaReddy', 'manu'];

changingArrayValues[0] = 'srimannarayana';
console.log(changingArrayValues[0]);

//maniplatimg arrays

// adding and reomoving  the elements on end of the aarray with help of--- push() and pop()

const addingElementsInArray = ['a', 'b', 'c'];

addingElementsInArray.push('d', 'e');    //using push()

console.log(addingElementsInArray);

const removingElementInArray = ['a', 'b', 'c'];

removingElementInArray.pop();  //pop()

console.log(removingElementInArray);

//adding and reomoving  the elements beginning of the aarray with help of---  shift() and unshift()

const addingElementsInArray1 = ['a', 'b', 'c'];

 addingElementsInArray1.shift();    //using shift()

console.log( addingElementsInArray1);

const removingElementInArray1 = ['a', 'b', 'c'];

removingElementInArray1.unshift('sriman');   //add more elements in array by using unshift        //unshift

console.log(removingElementInArray1);



